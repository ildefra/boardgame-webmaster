// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.m4.bgw.domain;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.m4.bgw.domain.AchievementTranslation;
import org.m4.bgw.domain.AchievementTranslationDataOnDemand;
import org.m4.bgw.domain.AchievementTranslationIntegrationTest;
import org.m4.bgw.domain.AchievementTranslationPK;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AchievementTranslationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AchievementTranslationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AchievementTranslationIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AchievementTranslationIntegrationTest: @Transactional;
    
    @Autowired
    AchievementTranslationDataOnDemand AchievementTranslationIntegrationTest.dod;
    
    @Test
    public void AchievementTranslationIntegrationTest.testCountAchievementTranslations() {
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to initialize correctly", dod.getRandomAchievementTranslation());
        long count = AchievementTranslation.countAchievementTranslations();
        Assert.assertTrue("Counter for 'AchievementTranslation' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AchievementTranslationIntegrationTest.testFindAchievementTranslation() {
        AchievementTranslation obj = dod.getRandomAchievementTranslation();
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to initialize correctly", obj);
        AchievementTranslationPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to provide an identifier", id);
        obj = AchievementTranslation.findAchievementTranslation(id);
        Assert.assertNotNull("Find method for 'AchievementTranslation' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AchievementTranslation' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AchievementTranslationIntegrationTest.testFindAllAchievementTranslations() {
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to initialize correctly", dod.getRandomAchievementTranslation());
        long count = AchievementTranslation.countAchievementTranslations();
        Assert.assertTrue("Too expensive to perform a find all test for 'AchievementTranslation', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AchievementTranslation> result = AchievementTranslation.findAllAchievementTranslations();
        Assert.assertNotNull("Find all method for 'AchievementTranslation' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AchievementTranslation' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AchievementTranslationIntegrationTest.testFindAchievementTranslationEntries() {
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to initialize correctly", dod.getRandomAchievementTranslation());
        long count = AchievementTranslation.countAchievementTranslations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AchievementTranslation> result = AchievementTranslation.findAchievementTranslationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AchievementTranslation' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AchievementTranslation' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AchievementTranslationIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to initialize correctly", dod.getRandomAchievementTranslation());
        AchievementTranslation obj = dod.getNewTransientAchievementTranslation(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to provide a new transient entity", obj);
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'AchievementTranslation' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AchievementTranslationIntegrationTest.testRemove() {
        AchievementTranslation obj = dod.getRandomAchievementTranslation();
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to initialize correctly", obj);
        AchievementTranslationPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AchievementTranslation' failed to provide an identifier", id);
        obj = AchievementTranslation.findAchievementTranslation(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AchievementTranslation' with identifier '" + id + "'", AchievementTranslation.findAchievementTranslation(id));
    }
    
}
