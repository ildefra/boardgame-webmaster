// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.m4.bgw.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.m4.bgw.domain.GameTag;
import org.m4.bgw.domain.GameTagDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect GameTagDataOnDemand_Roo_DataOnDemand {
    
    declare @type: GameTagDataOnDemand: @Component;
    
    private Random GameTagDataOnDemand.rnd = new SecureRandom();
    
    private List<GameTag> GameTagDataOnDemand.data;
    
    public GameTag GameTagDataOnDemand.getNewTransientGameTag(int index) {
        GameTag obj = new GameTag();
        return obj;
    }
    
    public GameTag GameTagDataOnDemand.getSpecificGameTag(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        GameTag obj = data.get(index);
        String id = obj.getName();
        return GameTag.findGameTag(id);
    }
    
    public GameTag GameTagDataOnDemand.getRandomGameTag() {
        init();
        GameTag obj = data.get(rnd.nextInt(data.size()));
        String id = obj.getName();
        return GameTag.findGameTag(id);
    }
    
    public boolean GameTagDataOnDemand.modifyGameTag(GameTag obj) {
        return false;
    }
    
    public void GameTagDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = GameTag.findGameTagEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'GameTag' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<GameTag>();
        for (int i = 0; i < 10; i++) {
            GameTag obj = getNewTransientGameTag(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
