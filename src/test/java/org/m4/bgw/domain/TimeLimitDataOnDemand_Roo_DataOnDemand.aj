// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.m4.bgw.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.m4.bgw.domain.TimeLimit;
import org.m4.bgw.domain.TimeLimitDataOnDemand;
import org.springframework.stereotype.Component;

privileged aspect TimeLimitDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TimeLimitDataOnDemand: @Component;
    
    private Random TimeLimitDataOnDemand.rnd = new SecureRandom();
    
    private List<TimeLimit> TimeLimitDataOnDemand.data;
    
    public TimeLimit TimeLimitDataOnDemand.getNewTransientTimeLimit(int index) {
        TimeLimit obj = new TimeLimit();
        setName(obj, index);
        setTurnBased(obj, index);
        return obj;
    }
    
    public void TimeLimitDataOnDemand.setName(TimeLimit obj, int index) {
        String name = "name_" + index;
        if (name.length() > 50) {
            name = name.substring(0, 50);
        }
        obj.setName(name);
    }
    
    public void TimeLimitDataOnDemand.setTurnBased(TimeLimit obj, int index) {
        Boolean turnBased = true;
        obj.setTurnBased(turnBased);
    }
    
    public TimeLimit TimeLimitDataOnDemand.getSpecificTimeLimit(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        TimeLimit obj = data.get(index);
        Integer id = obj.getTimeLimitId();
        return TimeLimit.findTimeLimit(id);
    }
    
    public TimeLimit TimeLimitDataOnDemand.getRandomTimeLimit() {
        init();
        TimeLimit obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getTimeLimitId();
        return TimeLimit.findTimeLimit(id);
    }
    
    public boolean TimeLimitDataOnDemand.modifyTimeLimit(TimeLimit obj) {
        return false;
    }
    
    public void TimeLimitDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = TimeLimit.findTimeLimitEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'TimeLimit' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<TimeLimit>();
        for (int i = 0; i < 10; i++) {
            TimeLimit obj = getNewTransientTimeLimit(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
