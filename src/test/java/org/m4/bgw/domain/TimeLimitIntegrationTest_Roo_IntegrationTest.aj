// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.m4.bgw.domain;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.m4.bgw.domain.TimeLimit;
import org.m4.bgw.domain.TimeLimitDataOnDemand;
import org.m4.bgw.domain.TimeLimitIntegrationTest;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect TimeLimitIntegrationTest_Roo_IntegrationTest {
    
    declare @type: TimeLimitIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: TimeLimitIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: TimeLimitIntegrationTest: @Transactional;
    
    @Autowired
    TimeLimitDataOnDemand TimeLimitIntegrationTest.dod;
    
    @Test
    public void TimeLimitIntegrationTest.testCountTimeLimits() {
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to initialize correctly", dod.getRandomTimeLimit());
        long count = TimeLimit.countTimeLimits();
        Assert.assertTrue("Counter for 'TimeLimit' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void TimeLimitIntegrationTest.testFindTimeLimit() {
        TimeLimit obj = dod.getRandomTimeLimit();
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to initialize correctly", obj);
        Integer id = obj.getTimeLimitId();
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to provide an identifier", id);
        obj = TimeLimit.findTimeLimit(id);
        Assert.assertNotNull("Find method for 'TimeLimit' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'TimeLimit' returned the incorrect identifier", id, obj.getTimeLimitId());
    }
    
    @Test
    public void TimeLimitIntegrationTest.testFindAllTimeLimits() {
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to initialize correctly", dod.getRandomTimeLimit());
        long count = TimeLimit.countTimeLimits();
        Assert.assertTrue("Too expensive to perform a find all test for 'TimeLimit', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<TimeLimit> result = TimeLimit.findAllTimeLimits();
        Assert.assertNotNull("Find all method for 'TimeLimit' illegally returned null", result);
        Assert.assertTrue("Find all method for 'TimeLimit' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void TimeLimitIntegrationTest.testFindTimeLimitEntries() {
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to initialize correctly", dod.getRandomTimeLimit());
        long count = TimeLimit.countTimeLimits();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<TimeLimit> result = TimeLimit.findTimeLimitEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'TimeLimit' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'TimeLimit' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void TimeLimitIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to initialize correctly", dod.getRandomTimeLimit());
        TimeLimit obj = dod.getNewTransientTimeLimit(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'TimeLimit' identifier to be null", obj.getTimeLimitId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'TimeLimit' identifier to no longer be null", obj.getTimeLimitId());
    }
    
    @Test
    public void TimeLimitIntegrationTest.testRemove() {
        TimeLimit obj = dod.getRandomTimeLimit();
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to initialize correctly", obj);
        Integer id = obj.getTimeLimitId();
        Assert.assertNotNull("Data on demand for 'TimeLimit' failed to provide an identifier", id);
        obj = TimeLimit.findTimeLimit(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'TimeLimit' with identifier '" + id + "'", TimeLimit.findTimeLimit(id));
    }
    
}
