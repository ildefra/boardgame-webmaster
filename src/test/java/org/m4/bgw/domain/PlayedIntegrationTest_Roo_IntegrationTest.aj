// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.m4.bgw.domain;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.m4.bgw.domain.Played;
import org.m4.bgw.domain.PlayedDataOnDemand;
import org.m4.bgw.domain.PlayedIntegrationTest;
import org.m4.bgw.domain.PlayedPK;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PlayedIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PlayedIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PlayedIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PlayedIntegrationTest: @Transactional;
    
    @Autowired
    PlayedDataOnDemand PlayedIntegrationTest.dod;
    
    @Test
    public void PlayedIntegrationTest.testCountPlayeds() {
        Assert.assertNotNull("Data on demand for 'Played' failed to initialize correctly", dod.getRandomPlayed());
        long count = Played.countPlayeds();
        Assert.assertTrue("Counter for 'Played' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PlayedIntegrationTest.testFindPlayed() {
        Played obj = dod.getRandomPlayed();
        Assert.assertNotNull("Data on demand for 'Played' failed to initialize correctly", obj);
        PlayedPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Played' failed to provide an identifier", id);
        obj = Played.findPlayed(id);
        Assert.assertNotNull("Find method for 'Played' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Played' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PlayedIntegrationTest.testFindAllPlayeds() {
        Assert.assertNotNull("Data on demand for 'Played' failed to initialize correctly", dod.getRandomPlayed());
        long count = Played.countPlayeds();
        Assert.assertTrue("Too expensive to perform a find all test for 'Played', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Played> result = Played.findAllPlayeds();
        Assert.assertNotNull("Find all method for 'Played' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Played' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PlayedIntegrationTest.testFindPlayedEntries() {
        Assert.assertNotNull("Data on demand for 'Played' failed to initialize correctly", dod.getRandomPlayed());
        long count = Played.countPlayeds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Played> result = Played.findPlayedEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Played' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Played' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PlayedIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Played' failed to initialize correctly", dod.getRandomPlayed());
        Played obj = dod.getNewTransientPlayed(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Played' failed to provide a new transient entity", obj);
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Played' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PlayedIntegrationTest.testRemove() {
        Played obj = dod.getRandomPlayed();
        Assert.assertNotNull("Data on demand for 'Played' failed to initialize correctly", obj);
        PlayedPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Played' failed to provide an identifier", id);
        obj = Played.findPlayed(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Played' with identifier '" + id + "'", Played.findPlayed(id));
    }
    
}
