// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.m4.bgw.web;

import java.io.UnsupportedEncodingException;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.m4.bgw.Language;
import org.m4.bgw.domain.LevelTranslation;
import org.m4.bgw.domain.LevelTranslationPK;
import org.m4.bgw.domain.UserLevel;
import org.m4.bgw.web.LevelTranslationController;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.convert.ConversionService;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect LevelTranslationController_Roo_Controller {
    
    private ConversionService LevelTranslationController.conversionService;
    
    @Autowired
    public LevelTranslationController.new(ConversionService conversionService) {
        super();
        this.conversionService = conversionService;
    }

    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String LevelTranslationController.create(@Valid LevelTranslation levelTranslation, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, levelTranslation);
            return "leveltranslations/create";
        }
        uiModel.asMap().clear();
        levelTranslation.persist();
        return "redirect:/leveltranslations/" + encodeUrlPathSegment(conversionService.convert(levelTranslation.getId(), String.class), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String LevelTranslationController.createForm(Model uiModel) {
        populateEditForm(uiModel, new LevelTranslation());
        return "leveltranslations/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String LevelTranslationController.show(@PathVariable("id") LevelTranslationPK id, Model uiModel) {
        uiModel.addAttribute("leveltranslation", LevelTranslation.findLevelTranslation(id));
        uiModel.addAttribute("itemId", conversionService.convert(id, String.class));
        return "leveltranslations/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String LevelTranslationController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, @RequestParam(value = "sortFieldName", required = false) String sortFieldName, @RequestParam(value = "sortOrder", required = false) String sortOrder, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("leveltranslations", LevelTranslation.findLevelTranslationEntries(firstResult, sizeNo, sortFieldName, sortOrder));
            float nrOfPages = (float) LevelTranslation.countLevelTranslations() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("leveltranslations", LevelTranslation.findAllLevelTranslations(sortFieldName, sortOrder));
        }
        return "leveltranslations/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String LevelTranslationController.update(@Valid LevelTranslation levelTranslation, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, levelTranslation);
            return "leveltranslations/update";
        }
        uiModel.asMap().clear();
        levelTranslation.merge();
        return "redirect:/leveltranslations/" + encodeUrlPathSegment(conversionService.convert(levelTranslation.getId(), String.class), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String LevelTranslationController.updateForm(@PathVariable("id") LevelTranslationPK id, Model uiModel) {
        populateEditForm(uiModel, LevelTranslation.findLevelTranslation(id));
        return "leveltranslations/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String LevelTranslationController.delete(@PathVariable("id") LevelTranslationPK id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        LevelTranslation levelTranslation = LevelTranslation.findLevelTranslation(id);
        levelTranslation.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/leveltranslations";
    }
    
    void LevelTranslationController.populateEditForm(Model uiModel, LevelTranslation levelTranslation) {
        uiModel.addAttribute("levelTranslation", levelTranslation);
        uiModel.addAttribute("languages", Language.findAllLanguages());
        uiModel.addAttribute("userlevels", UserLevel.findAllUserLevels());
    }
    
    String LevelTranslationController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
